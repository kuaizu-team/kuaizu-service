type: object
required:
  - commissionRate
properties:
  schoolId:
    type: integer
    description: Required when the target is a multi-school super admin.
  commissionRate:
    type: number
    format: double
    minimum: 0
    maximum: 100
    description: Commission rate percentage, e.g. 30.5 means 30.5%
