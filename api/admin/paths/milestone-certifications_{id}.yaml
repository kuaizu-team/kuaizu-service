parameters:
  - in: path
    name: id
    required: true
    schema: { type: integer }
patch:
  tags: [Projects]
  summary: 审核时间节点认证（平台超级管理员）
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          required: [approved]
          properties:
            approved: { type: boolean }
  responses:
    '200':
      description: 审核完成且佐证图片已删除
