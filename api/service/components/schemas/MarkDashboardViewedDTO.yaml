type: object
properties:
  type:
    type: string
    enum: [like, favorite, share, visit, entry]
    description: entry marks only the outer dashboard badge; omitted type retains legacy mark-all behavior.
