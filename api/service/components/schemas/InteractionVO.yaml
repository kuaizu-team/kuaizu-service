type: object
required: [liked, favorited, shared, likeCount, favoriteCount, shareCount]
properties:
  active: { type: boolean }
  liked: { type: boolean }
  favorited: { type: boolean }
  shared: { type: boolean }
  likeCount: { type: integer }
  favoriteCount: { type: integer }
  shareCount: { type: integer }
