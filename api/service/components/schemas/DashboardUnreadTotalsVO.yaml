type: object
required: [projectCount, talentCount, totalCount]
properties:
  projectCount: { type: integer, minimum: 0 }
  talentCount: { type: integer, minimum: 0 }
  totalCount: { type: integer, minimum: 0 }
