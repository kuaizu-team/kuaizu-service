parameters:
  - name: id
    in: path
    required: true
    schema: { type: integer }
post:
  summary: Toggle interaction atomically
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            allOf:
              - $ref: ../components/schemas/BaseResponse.yaml
              - type: object
                properties:
                  data: { $ref: ../components/schemas/InteractionVO.yaml }
