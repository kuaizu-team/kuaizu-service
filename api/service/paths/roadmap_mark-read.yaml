post:
  tags:
    - Roadmap
  summary: Mark roadmap updates as read
  operationId: markRoadmapRead
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            $ref: ../components/schemas/BaseResponse.yaml
