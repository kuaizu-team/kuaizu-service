parameters:
  - name: id
    in: path
    required: true
    schema: { type: integer }
get:
  summary: Get interaction state and cumulative counts
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            allOf:
              - $ref: ../components/schemas/BaseResponse.yaml
              - type: object
                properties:
                  data: { $ref: ../components/schemas/InteractionVO.yaml }
