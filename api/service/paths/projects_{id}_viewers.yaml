parameters:
  - name: id
    in: path
    required: true
    schema:
      type: integer
    description: Project ID
get:
  tags:
    - Projects
  summary: Get project viewers
  description: Only the project creator can view authenticated viewers from the last 24 hours.
  operationId: getProjectViewers
  parameters:
    - $ref: ../components/parameters/PageParam.yaml
    - name: limit
      in: query
      description: Max rows to return, capped at 50.
      schema:
        type: integer
        minimum: 1
        maximum: 50
        default: 20
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            allOf:
              - $ref: ../components/schemas/BaseResponse.yaml
              - type: object
                properties:
                  data:
                    type: object
                    properties:
                      total:
                        type: integer
                      list:
                        type: array
                        items:
                          type: object
                          properties:
                            user_id:
                              type: integer
                            talent_profile_id:
                              type: integer
                              nullable: true
                            nickname:
                              type: string
                              nullable: true
                            avatar_url:
                              type: string
                              nullable: true
                            auth_status:
                              type: integer
                            collaboration_score:
                              type: number
                              format: double
                              nullable: true
                            last_viewed_at:
                              type: string
                              format: date-time
